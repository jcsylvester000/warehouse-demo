[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# Single-page app: send all routes to index.html so deep links work
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
